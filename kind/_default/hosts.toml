# see https://github.com/containerd/containerd/blob/main/core/remotes/docker/config/hosts.go#L318 for a complete list of options
[host."/run/containerd/host_containerd.sock"]
  capabilities = ["pull", "resolve"]
  skip_verify = true